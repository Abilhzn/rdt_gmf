# Secrets — JANGAN PERNAH commit
.env
.env.*
confidential.txt

# Dependencies
node_modules/
package-lock.json.bak

# Build & runtime artifacts
dist/
build/
*.log
*.log.err

# /graphify output — regenerable knowledge-graph artifact (cache, graph.html/json, manifest),
# not source. Untracked since sesi 8 (12 Agu); decided sesi 9 to gitignore rather than commit.
graphify-out/

# Backend runtime data (bukan source, jangan ikut commit)
backend_run_output.txt
backend_server.log
backend_server.log.err

# Checklist 2.2 (12 Agu): logger.js's error.log, per service — *.log di atas sudah nutup ini juga,
# eksplisit di sini biar jelas kalau ada yang cari.
auth/logs/
data_user/logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

.claude/
project/budgeting_gmf/rdt/docs/*