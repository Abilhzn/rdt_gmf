{
  "name": "auth",
  "version": "0.1.0",
  "private": true,
  "description": "Login/session (authentication) service — shared across budgeting_gmf apps (rdt, future ibt). Provisional/synthetic, see TODO(IT-AUTH) in src/index.js. Authorization (dinas/role access rules) stays app-side, not here.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
