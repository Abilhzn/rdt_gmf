# Session 14 — 20 Agustus 2026

**Branch**: `it-handoff-demo` (baru, dari `lenovo`) — **jangan disentuh**
`lenovo`/`main` sepanjang sesi ini per instruksi eksplisit user. 5 commit,
sudah di-push ke `origin/it-handoff-demo` (branch baru di remote, PR belum
dibuka).

## 1. IT handoff demo prep (5 poin, urgent siang)

User minta persiapan presentasi + serah kode ke tim IT GMF besok paginya.
5 poin, dikerjakan berurutan:

1. **Trim paragraf deskripsi teknis di UI** — semua `<p class="note">`
   panjang yang nyebut REQ-ID/tanggal/narasi keputusan development di
   halaman Dashboard, Confirm, Need Approval, Setting Periode, Repost
   History, Share-Cost. Label/placeholder/pesan error fungsional
   dipertahankan.
2. **Kredensial sintetis di-redact** — `auth/src/credentials.seed.json`
   dikosongkan (cuma comment), tapi mekanisme generate-nya dipertahankan
   lewat 2 script baru: `auth/tools/generateCredentials.js`,
   `data_user/tools/generateEmployeeDirectory.js`. `verifyCredentials()`
   fallback generate in-memory kalau file kosong, jadi login + test suite
   `auth` tetap jalan tanpa perlu regen manual dulu.
3. **Rapikan komentar kode** — sempat 2 ronde (lihat bagian 2 di bawah,
   ronde 1 di sini gak lengkap).
4. **Cek hygiene secret** — `.gitignore` dikonfirmasi exclude `.env`/
   `confidential.txt`, `git log -p --all` full history scan: nol secret
   asli ketemu (cuma placeholder/synthetic).
5. **Sample Excel Share-Cost** — generator baru
   `rdt/backend/tools/generateShareCostSampleFile.js`, verified parse
   jadi 4 baris PENDING TC→TAB (format lama waktu itu, sebelum migrasi
   Format CBO di bagian 6).

**Commit**: `d5e6ca7`.

## 2. Ronde 2: audit ulang komentar (setelah user cek manual, nemu yang kelewat)

User bilang "masih ada paragraf deskripsi teknis yang kelewat" — diminta
audit ULANG SELURUH aplikasi, jangan cuma percaya laporan sesi
sebelumnya. Ketemu akar masalahnya: sesi 1's daftar file untuk background
agent cuma isi `.ts` dari 7 komponen (confirm/dashboard-detail/home/
need-approval/repost-history/setting-periode/share-cost) — **`.html`
template-nya kelewat total**, plus SCSS gak pernah masuk scope sama
sekali.

Laporan disusun sebagai Artifact (tabel sebelum/sesudah per file) buat
di-review user dulu sebelum apply — user approve semuanya termasuk
bagian SCSS ("ga ada pengecualian"). Dikerjakan: ~40 blok komentar di 7
file `.html` (manual, langsung sendiri, bukan lewat agent — biar
konsisten), 82 baris di 13 file `.scss` (background agent terpisah), plus
beberapa baris nyasar di `.ts`/`.js`.

**Commit**: `8c07588` — 25 file, +282/-436.

## 3. Migrasi database: Supabase → PostgreSQL lokal

User pindahin DB balik ke Postgres lokal (`rdt_dev`). Kerjaan: update
`rdt/backend/.env` (via `sed -i`, TIDAK pernah baca isi filenya — permission
settings user block read langsung, dihormati), jalanin `migrate.js`
(schema + semua migration), verifikasi end-to-end lewat login+dashboard
real request chain (auth+data_user+rdt/backend+Postgres).

**Temuan penting**: cek soal SSL "hardcode wajib nyala" dan `pg.Pool max`
yang katanya dikecilin buat Supabase free tier — **dua-duanya ternyata
gak ada di kode sama sekali**. Semua koneksi pakai `new Client({
connectionString })` polos, SSL murni dari `sslmode` di connection string
itu sendiri (bukan hardcode), dan cuma ada SATU `new Pool()` di seluruh
codebase (`seedShareCostSynthetic.js`), gak pernah di-`max`-in. User
konfirmasi asumsinya, tapi kodenya emang udah bersih — nol perubahan
kode diperlukan.

**Insiden**: user sempat truncate `rdt.transactions` manual, abis itu
login gagal 500 — ternyata bukan gara-gara truncate (schema/seed roster
utuh), tapi server auth/rdt-backend kebetulan lagi mati (saya matiin
proses test verifikasi Supabase-vs-local sebelumnya, lupa restart buat
kepake user). Direstart, semua normal.

**Insiden ke-2**: user curiga masih connect ke Supabase karena TAB
dashboard "ada isinya" padahal DB katanya fresh. Investigasi: `rdt_dev`
lokal itu memang database dev ASLI dari sebelum project pernah pindah ke
Supabase — udah ada 8865 transaksi dari sesi-sesi development lama,
`migrate.js` cuma apply schema/seed roster, gak pernah truncate data.
Dibuktikan dengan cross-check angka API-vs-query-langsung, match persis
— confirmed lokal, bukan Supabase.

Gak ada commit terpisah untuk bagian ini (murni infra/config, `.env`
di-gitignore).

## 4. Prompt B: Format TAB output (opsi export baru)

SRS.md ada update "TERJAWAB 15 Agu" — file resmi `Format_Detail_Transaksi.xlsx`
dari IT diterima, isinya 2 template: `Format CBO` (input, 12 kolom,
`Recipient` eksplisit) dan `Format TAB` (output, 8 kolom, `Qty`/`UoM`
fixed + `Text` CONCATENATE). Ada 1 pertanyaan terbuka waktu itu: apakah
Format CBO GANTIIN format lama atau BERDAMPINGAN — user eksplisit minta
**cuma kerjain sisi OUTPUT dulu** (aman terlepas dari jawaban itu), sisi
INPUT nunggu.

**Dikerjakan**: `buildFormatTabWorkbookBuffer()` baru di
`exportBatches.js`, opsi `?format=tab` di 2 route export yang sudah ada
(gak gantiin export 53-kolom lama). Tombol baru "Download (Format TAB)"
di halaman Wait to Repost. Diverifikasi terhadap template resmi — header
byte-identik termasuk trailing space `"Text "`, lalu re-verified pakai
data real dari TB.

**Commit**: `51a38e4` — 9 file, termasuk retire 3 test yang gantung ke
`06. DT TJ JUN 2026 R1.xlsx` (fixture yang user hapus, dikonfirmasi
sengaja, bukan nunggu diganti).

Terpisah, user juga confirm IRS.md/SRS.md update + file Share-Cost sample
yang dia convert manual ke Format CBO itu authorship-nya dia — di-commit
sebagai **`043c2cc`**.

## 5. Keputusan besar: Format CBO GANTIIN format lama — parser di-rewrite total

User jawab pertanyaan terbuka: **format baru GANTIIN yang lama**, bukan
berdampingan. Ditemukan dulu kenapa file TJ/Share-Cost yang user convert
manual jadi 0-baris kalau diupload (gerbang deteksi sheet lama butuh
kolom `Cost Ctr`+`Value Date` yang gak ada di Format CBO 12-kolom).

User eksplisit minta simplifikasi agresif: "langsung baca excel aja, ga
usah liat dlu nama sheet... harusnya banyak baris code yang bisa
dihapus". `excelParser.js` ditulis ulang dari ~694 jadi ~245 baris —
dihapus total: deteksi pivot sheet (A3/A4 cell), pivot-cache reading,
fallback kolom `Review <dinas>`, sub-dinas suffix guessing
(`resolveSubDinasCode`), 45 dari 53 kolom kontrak lama yang gak ada lagi
di Format CBO. `dinas_target` sekarang baca langsung kolom `Recipient`
(tetap divalidasi lewat `mapping.seed.json`+roster, unresolved value
tetap `NEEDS_REVIEW`, gak silently misroute). `pivotCacheReader.js`
dihapus total (satu-satunya pemakainya).

**Aturan baru** (diminta user): baris dengan `Requester` == `Recipient`
(dinas sama di dua kolom itu) → `EXCLUDED`. Dicek dari kolom `Requester`
milik baris itu sendiri, bukan cuma `uploaderDinas` sesi login — ada test
yang sengaja bikin dua-duanya beda biar bukti logic barunya yang jalan.

**Bug ketemu gak sengaja**: pas testing manual, `POST /api/persist` malah
nge-CRASH SELURUH SERVER kalau `rows`/`aggregation` di body bukan JSON
valid (`JSON.parse` gak dibungkus try/catch) — user minta di-fix
sekalian. Sudah dibungkus, return `400` bersih, diverifikasi live (server
tetap hidup abis dikirim JSON rusak).

**Verifikasi**: test suite ditulis ulang pakai data REAL (bukan angka
karangan) — cross-check dulu angka baru vs breakdown GL-category lama
sebelum nulis assertion (TC 85312.21+9420=94732.21, dst — persis sama,
cuma jalur derivasinya beda). Full round-trip lewat server nyata untuk
ketiga contoh input (TB 469 baris, TJ 490 baris, Share-Cost 4 baris) —
parse DAN persist ke Postgres, data test dibersihin lagi abis verifikasi
(termasuk cascade cleanup notifications→comments→transactions→uploads).
`npm test` 100/100, `ng build` bersih.

Frontend cleanup ikutan (kode mati permanen): kolom preview "Sub Group" +
badge "Agregat Pivot" dihapus dari `confirm.component.ts`,
`repost-budgeting.component.ts/.html`, `transaction.model.ts`.

**Commit**: `d968df0` — 9 file, +171/-726.

## Known open items / backlog (carry-over)

- **`duplicateCheck.js` jadi kurang ketat** — natural key-nya gabungan 8
  field, 3 di antaranya (`document_no`, `cost_ctr`, `item`) gak ada lagi
  di Format CBO, sekarang selalu kosong. Deteksi duplikat cross-upload
  masih jalan (sisa 5 field), cuma gak seketat dulu. Sengaja TIDAK
  disentuh — nyentuh logic keamanan data, nunggu arahan eksplisit user.
- **`Format_Detail_Transaksi.xlsx`** (template resmi dari IT) sudah gak
  ada lagi di `contoh_input/` langsung — user pindahin ke
  `contoh_input/original/` (folder backup baru berisi versi lama TB/TJ +
  template ini). Gak ada test yang butuh file ini secara langsung.
- **Belum ada PR dibuka** — branch baru ke-push, GitHub kasih link buat
  bikin PR tapi belum diminta user.
- File-file berikut masih untracked, di luar scope sesi ini (carry-over
  lama, konsisten sesi-sesi sebelumnya): `seedShareCostSynthetic.js`,
  `progress_log/11_2026-08-14.md`.
- Auth/data_user masih provisional/synthetic (TODO(IT-AUTH)) — carry-over
  lama, gak berubah sesi ini.

## Catatan proses

Ditulis di akhir sesi atas permintaan user langsung ("push it dan buat
progress log buat hari ini"). Sesi ini panjang dan lintas-topik (5 task
besar berbeda dalam satu hari) — dirangkum per-topik kronologis di atas
supaya kalau perlu lanjut kerja soal salah satu (misal `duplicateCheck.js`
atau Format TAB), konteksnya ketemu cepat tanpa re-baca transcript penuh.
