# Session 7 — 7–11 Agustus 2026

**Branch**: started on `rdt/ui-batch-5-agu-symmetry-sticky-cols`, ended on `lenovo`
(new device-tracking branch, forked from the same tip). Both pushed;
**`main` fast-forwarded to include everything, pushed 11 Agu** — first time
this session's work reached `main`.

This session ran across several real-world days with two pauses (weekly
usage limit on 7 Agu, an accidental laptop shutdown on 11 Agu) — covered
here as one continuous log since it's one continuous Claude Code
conversation (never `/clear`d until after this file was written).

## Arc A — ui-demo.html removal + chat-bubble fix (7 Agu, early)

1. **`ui-demo.html` deleted outright** (not synced) — the standalone
   vanilla-JS frontend that used to run alongside Angular, per explicit
   user instruction ("kedepannya kita bakal langsung ngedit frontend di
   Angular"). Backend route serving it removed (`index.js`), `start-all.bat`
   updated, `rdt/README.md`/`rdt/CLAUDE.md`/`rdt/docs/SRS.md` all revised to
   drop the dual-frontend framing, ~30 stale code comments cleaned up
   across ~20 files (delegated to the `worker` subagent — mechanical, no
   logic touched).
2. **Chat-bubble icon fix** — pair-card "N comment" text had no icon at
   all (not cosmetic, per user: needed to actually see comment counts).
   Added the same inline Lucide `message-square` SVG the sidebar nav
   already used.
3. Branch pushed, **deliberately not merged to `main`** at that point (per
   standing instruction at the time).
4. `figma-fresh-compare` branch (an unsatisfying design experiment)
   deleted per user request, distinguished from the real feature branch.

## Arc B — REQ-RDT-SAP-14 "Setting Periode" restructure + Bagian 2 batch (7–11 Agu)

SRS.md section 3.3 gained two new subsections ("DIPERJELAS 7 Agu",
"Feedback tambahan 7 Agu") the user wrote directly — this arc implements
them. Full plan went through `EnterPlanMode`/`ExitPlanMode` with explicit
approval, `senior-advisor` review (touches the `periode_efektif` snapshot
mechanism — audit-sensitive), and an `AskUserQuestion` to resolve one real
open design question before implementation. **Paused after task #3** (weekly
limit hit, 7 Agu) — resumed and finished all 11 tasks 11 Agu.

**Bagian 1 (sensitive)**: "Kelola Deadline Periode" renamed to "Setting
Periode", 2 sub-sections: "Setting Deadline" (bulk, unchanged) and a
rebuilt "Override Deadline" — was a manual dinas-picker form, now
list-driven. New `GET /api/period-deadlines/overdue?periode=YYYY-MM` (TAB
sees pasangan that are 100% confirmed but overdue and still un-batched —
scoped to `export_batch_id IS NULL`, confirmed via `AskUserQuestion`: an
already-archived pasangan can never appear here) and new `POST
/api/period-deadlines/override-reevaluate` (re-opens one pasangan with a
new deadline — re-checks everything inside a transaction after
`ORDER BY id ASC FOR UPDATE` row locks, re-evaluates `periode_efektif` PER
ROW, writes one `audit_log` row per transaction — the one deliberate
exception to the snapshot being permanent). Verified live via a 5-scenario
DB script (overdue-when-unbatched, hidden-when-batched,
hidden-when-periode-N+1-set, override actually shifts data + audit trail,
rejects an ineligible pasangan) — all passed.

**Bagian 2 (5 independent items)**:
1. Dashboard TAB's per-dinas rollup "Status" column was empty for the
   common in-progress case (backend only ever set it for 2 of 4 real
   cases) — fixed + matching CSS tones added.
2. Riwayat Repost rows now link to Dashboard-Detailing's existing
   thread+chain view for archived pairs — turned out to be a
   frontend-only gap, the backend already supported it.
3. **REQ-RDT-SAP-09 (auto-archive) verification found a real bug**:
   `buildChainAwareProgress` (powers "Own Repost", both personal and TAB
   global view) had no `export_batch_id` filter at all — a fully-reposted
   pair never disappeared from that one queue, unlike Need Approval/Need
   to Confirm which were already correct. Fixed by filtering the
   aggregated cards post-hoc (the row query itself still needs unfiltered
   data for the partial-repost/multi-subdoc tracking) — verified live,
   twice (once on the old local DB, re-verified again after the Supabase
   migration below).
4. Dashboard TAB symmetry audit — no reference screenshot this round (see
   Arc C), audited against SRS's own documented design tokens instead;
   found and fixed one concrete mismatch (`.rollup-status` pill radius).
5. "Deskripsi (opsional)" and sibling textareas were growing on every
   keystroke instead of staying a fixed size with internal scroll — added
   an `autoGrow` input to the shared `MentionInputComponent` (default
   `true`, so the separate Catatan Reviewer field's opposite requirement
   stays untouched), set `false` + fixed height on all 5 `.batch-note`
   call sites.

Committed as `6fe8292`, pushed to the feature branch. `npm test` 75/75,
`tsc --noEmit` clean, Angular vitest green throughout.

## Arc C — standing instruction: no more screenshot verification (7 Agu)

User: stop proving visual changes with browser screenshots going forward —
just describe the change in writing, they'll check it themselves. Reverses
the same-day-earlier "screenshot wajib" token-management note. Saved as a
memory (`no_more_screenshot_verification`); Bagian 2 item 4 above is the
first task actually affected by it.

## Arc D — repo/branch audit + Supabase migration (11 Agu)

User asked for a local-vs-GitHub comparison. Found:
- Feature branch: identical local/remote, no drift.
- **`main` was 9 commits behind `origin/main`** — those commits existed
  locally and on the pushed feature branch, just never reached `origin/main`
  as its own ref.
- **A `develop` branch existed on GitHub that this session never created**
  — turned out to be the user's own, made from a different PC ("pc lab"),
  identical content to the old `origin/main` tip.

Per user request: `develop` renamed to **`pc-lab`** (`git push
origin origin/develop:refs/heads/pc-lab` + delete old ref) to make room for
a **`lenovo`** branch (this PC's own device branch, forked from the tip of
`rdt/ui-batch-5-agu-symmetry-sticky-cols`) — the intent going forward is
one branch per physical device. Checked out `lenovo` locally, tracking
`origin/lenovo`, and continued working there.

**Database moved to Supabase** (user's own infra decision, already
connected from "pc lab"): user pasted a Supabase pooler connection string
directly in chat and updated `rdt/backend/.env` themselves — **this
session was permission-blocked from reading/writing that file directly**
(both Bash and the Read tool refused, a deliberate guardrail), so the user
did the edit manually and Claude only restarted the backend + ran
read-only verification afterward (Node's own `dotenv` loading the file
internally is fine and was used throughout — just never had the value
echoed to any visible output). Confirmed: connected to the right Supabase
host, `rdt` schema fully present (13 tables, including `period_deadlines`
from this session's own migration — schema is current), 490 real
transactions / 28 dinas already seeded from "pc lab"'s prior work. Fixed
one point of user confusion: Supabase Studio's Table Editor defaults to
showing only the `public` schema — the app's tables live under `rdt`, need
the schema switcher to see them.

Re-ran both this session's DB verification passes (SAP-09 fix, Override
Deadline 5 scenarios) live against the actual Supabase DB for extra
assurance, using pre-existing real dinas pairs the script picked
automatically (avoiding any pair with real pending data) — all passed,
synthetic rows cleaned up, confirmed zero residue afterward.

**`main` fast-forwarded to `6fe8292` and pushed** (11 Agu, explicit user
instruction) — clean fast-forward, `main` was already a strict ancestor of
`lenovo`'s tip, no merge conflicts. First time this session's cumulative
work (Arc A + B) reached `main` on GitHub.

Session ended with all 4 local services restarted (had been killed by an
accidental laptop shutdown) — `data_user` (4002), `auth` (4001), `rdt`
backend (4000, now pointed at Supabase), Angular dev-shell (4200) — all
verified healthy before this log was written.

## Known open items / backlog (unrelated to this session's own work)

- Sidebar collapsed/hover-expand (REQ-RDT-UI-06 diperluas) — still
  suspended, blocked on an accessible Dribbble reference. Was deferred to
  "Minggu malam" earlier; that passed without it coming back up — ask the
  user directly next time rather than silently re-deferring.
- `graphify-out/` still untracked in the repo, still undecided whether it
  should be gitignored or committed.
- Auth/data_user still provisional/synthetic (TODO(IT-AUTH)) — waiting on
  GMF IT's real employee/user table.
- Local `.claude/plans/resilient-herding-fairy.md` has the full technical
  plan for Arc B if ever needed for reference — the branch itself is now
  the source of truth, that file won't be kept updated further.
